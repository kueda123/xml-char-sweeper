# XML Char Sweeper (xml-char-sweeper)

IBM WAS Liberty などの構成ファイル（`server.xml`）に含まれる日本語文字（コメント等）による、文字化けや起動エラーを回避するためのクリーニングツールです。

## 📌 目的 (Purpose)

Windows環境で編集された `server.xml` に含まれるマルチバイト文字（日本語コメントなど）を除去、または正規化し、サーバーランタイムが正しく読み込める形式に変換します。

* **文字化け対策:** Shift_JIS/UTF-8の混在による読み込みエラーを防止。
* **自動バックアップ:** 処理実行時に元のファイルをバックアップします。
* **Gitフレンドリー:** GitHub上での表示崩れと、Windows上での実行互換性を両立した構成になっています。

## 📂 ファイル構成

| ファイル名 | 拡張子 | 必須エンコード | 役割 |
| :--- | :--- | :--- | :--- |
| **xml-char-sweeper.bat** | `.bat` | **UTF-8 (BOMなし)** | ツール本体。PowerShellをラップしたハイブリッドバッチ。 |
| **Gen-DirtyTestFile.ps1** | `.ps1` | **UTF-8 (BOM付き)** | テストデータ生成用スクリプト。 |
| **server.xml** | `.xml` | - | 処理対象のファイル（スクリプトと同じ階層に配置）。 |

## 🚀 使い方 (Usage)

### 1. クリーニングの実行
`xml-char-sweeper.bat` をダブルクリック、またはターミナルから実行してください。

```cmd
> .\xml-char-sweeper.bat